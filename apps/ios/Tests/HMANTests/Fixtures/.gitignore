# Regenerated by tests on every run — committing the live values would
# create constant noise. The placeholder ios_signature_fixture.json is
# tracked so the desktop verifier can locate the file before swift test
# has populated it; the desktop_signature_fixture.json is purely runtime.
desktop_signature_fixture.json
